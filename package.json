{
  "name": "whatsapp-automation-platform",
  "version": "1.0.0",
  "description": "Full-Stack Automated WhatsApp Outreach Platform",
  "scripts": {
    "build:frontend": "npm --prefix frontend install --include=dev && npm --prefix frontend run build",
    "build:backend": "npm --prefix backend install --include=dev && npm --prefix backend run build",
    "build": "npm run build:frontend && npm run build:backend",
    "start": "node backend/dist/server.js",
    "test": "npm --prefix backend test"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "whatsapp",
    "automation",
    "outreach",
    "marketing"
  ],
  "author": "",
  "license": "ISC"
}
